"""Private implementation details of the storage_manager package.

Nothing here is part of the public API; import from ``adsk.flow.local.storage_manager``
instead. Modules are grouped under this private sub-package so the package's
public surface (manager, models, component, config, exceptions) stays obvious at
the top level.
"""

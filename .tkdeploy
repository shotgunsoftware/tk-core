[packaging]
exclude=
    # Remove CI files.
    .coveragerc
    .flake8
    .hound.yml
    .pre-commit-config.yaml
    azure-pipelines.yml
    build_resources.yml
    codecov.yml
    developer
    # Remove unneeded files.
    docs
    tests

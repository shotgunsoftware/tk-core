# this is a comment at the top of the file
include: ./includes/empty_config.yml

engines:
  # this is a comment after the engines section
    test_engine:
        location: {'type': 'dev', 'path': '{PIPELINE_CONFIG}/config/bundles/test_engine'}
        debug_logging: false
        apps: {}

frameworks:
    test_framework_v1.x.x:
        location:
            type: dev
            path: '$TK_TEST_FIXTURES/config/bundles/test_framework_v1'
            version: v1.0.0

engines:
    test_engine:
        location: {'type': 'dev', 'path': '$TK_TEST_FIXTURES/config/bundles/test_engine'}
        debug_logging: false

        apps:
            test_app:
                # this is a comment in the test_app
                location: {'type': 'dev', 'path': '$TK_TEST_FIXTURES/config/bundles/test_app'}
